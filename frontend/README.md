# Frontend

React-based frontend for PulseCraft demo UI.

## Structure

- `src/` — React components and app logic
- `public/` — static assets
- `index.html` — entry point

## Run locally

From the frontend directory:

```powershell
npm install
npm run dev
```
