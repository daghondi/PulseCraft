# MVP: View Historical Results

Description

Allow users to view past demo sessions and results in a simple list or timeline UI.

Acceptance criteria

- The UI shows a list of saved sessions with timestamps.
- Clicking a session loads the replay data in the UI.
- Sessions persist between restarts (saved in `data/`).

Estimate: 3 hours

Owner: @frontend
